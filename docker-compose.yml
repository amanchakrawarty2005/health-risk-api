version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODEL_VERSION=v1.2.0
      - API_ENV=development
    volumes:
      - ./data:/app/data
      - ./mlruns:/app/mlruns
    restart: always